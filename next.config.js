/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['tailwindui.com', 'github.com', 'lh3.googleusercontent.com', 'realstatic.staticmania.com', 'images.unsplash.com'],
    }
}

module.exports = nextConfig
